input {
  beats {
    port => 5044
  }
}

filter {
  # Optional filters, e.g., parse JSON, system logs
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "filebeat-%{+YYYY.MM.dd}"
    user => "elastic"
    password => "${ELASTIC_PASSWORD}"
  }
}
